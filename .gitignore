# 密钥与本地数据，不入库
config/secrets.json
config/master.key
        #PII 加密主密钥（丢了就解不开历史数据，务必单独备份）
config/imap.secret
        #邮箱口令/授权码（界面上填，只落本地，绝不提交）
data/workbench.db
data/workbench.db-*
# 回收目录：界面上"删除"的来源简历落到这里（可恢复），属于本地数据不入库
data/removed/
# 自检/验收用的临时目录（跑完即删；万一中断也别提交进来）
data/_selftest_src/
data/_selftest_archive/
out/
__pycache__/
*.pyc

# Python 虚拟环境
.venv/

# ---------- 上传 GitHub 前的隐私加固（2026-09-23） ----------
# data 整目录不入库：库文件/简历原件（含真实简历）/备份/收信缓存全部属于本地数据
data/
# 邮箱配置里有真实账号；仓库提供 mailbox.example.json 模板
config/mailbox.json
.DS_Store
